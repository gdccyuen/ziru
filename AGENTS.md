# Agent skills

### Issue tracker

Local markdown under `.scratch/` (one directory per feature/effort; issues as numbered files with `Status:` lines). See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context layout — root `CONTEXT-MAP.md` points at `core/CONTEXT.md` (knowledge/account) and `webui/CONTEXT.md` (workspace/chat); ADRs live in `core/docs/adr/`. See `docs/agents/domain.md`.
