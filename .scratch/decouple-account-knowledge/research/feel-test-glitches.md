# Feel-test glitches (D3) — logged 2026-08-19, fixes scheduled for P7

1. **Composer (query box) sizing:** the query input box is too large — reduce height by ~half, smaller fonts; bottom icons/buttons should be flatter and shorter; the retrieval sliders (e.g. rerank) should be wider and more visually obvious.
2. **Thread management widget:** add a new/switch/delete chat-thread widget where the workspace switcher currently sits (design freedom: pick a suitable control — e.g. dropdown/command menu with create + list + delete). This implements Q34's thread management and replaces the removed workspace switcher.
